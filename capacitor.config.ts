import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'master_thesis',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
